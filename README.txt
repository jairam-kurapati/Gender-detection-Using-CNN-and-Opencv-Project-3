This folder should contain three subdirectories: 'train', 'validation', and 'test'.
Each of those should have one subdirectory per class (e.g., 'male', 'female') with image files (jpg/png) inside.
Place your dataset images in the appropriate folders before running `python train.py`.
